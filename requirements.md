# API遅延体感デモサイト 詳細仕様書

## 1. プロジェクト概要

### 1.1 目的
システム開発時にお客様へAPIの処理性能がUXに与える影響を体感してもらい、負荷試験の処理時間目標値を決定するためのデモツール。

### 1.2 コンセプト
- バックエンドAPIは実際には存在しない
- フロントエンドで指定されたミリ秒だけ待機し、擬似的にAPIレスポンス遅延を再現
- 3種類の代表的なWebサービス（EC・管理画面・SNS）をシミュレート
- 完全英語対応で正解中のユーザが活用できる

---

## 2. 技術スタック

| 項目 | 技術・バージョン |
|------|------------------|
| ビルドツール | Vite v7.3.1 |
| 言語 | TypeScript v5.9.3 |
| UIフレームワーク | React v19.2.4 |
| UIコンポーネント | shadcn/ui v3.7.0 |
| アイコン | Lucide React（shadcn/ui標準） |
| デプロイ先 | GitHub Pages |
| 対応デバイス | デスクトップ・タブレット・モバイル（レスポンシブ必須） |

---

## 3. デザイン仕様

### 3.1 デザインリファレンス
- **参照元**: https://ui.shadcn.com/examples/dashboard
- 上記ダッシュボードのレイアウト・スタイルを完全に踏襲する
- サイドバー、カード、テーブル、全体的なスペーシング・タイポグラフィを一致させる

### 3.2 テーマ
- ライトモード / ダークモード切り替え可能
- デフォルトはライトモード
- 切り替えトグルはヘッダー右上に配置（shadcn/uiダッシュボード例に準拠）

---

## 4. URL・ルーティング設計

### 4.1 URL構造

```
/{lang}/{page}?delay={ms}
```

| パラメータ | 説明 | 値 |
|-----------|------|-----|
| `lang` | 言語 | `jp` または `en` |
| `page` | 画面 | `ec`, `admin`, `sns` |
| `delay` | 遅延時間（ms） | 0〜10000（省略時: 500） |

### 4.2 URL例

```
/jp/ec?delay=1000      # 日本語・ECサイト・1000ms遅延
/en/admin?delay=2000   # 英語・管理画面・2000ms遅延
/jp/sns?delay=500      # 日本語・SNS・500ms遅延
/jp/ec                 # delay省略時はデフォルト500ms
```

### 4.3 ルーティング動作
- GitHub Pagesはハッシュルーター使用（`/#/jp/ec?delay=1000`形式）
- URLを直接変更 → 画面・言語トグル・遅延設定が同期
- UI操作（トグル・スライダー） → URLが自動更新
- 不正なURLはデフォルト（`/jp/ec?delay=500`）にリダイレクト

---

## 5. 画面構成

### 5.1 全体レイアウト

```
┌─────────────────────────────────────────────────────────────┐
│ ヘッダー（ロゴ・タイトル・テーマ切替）                          │
├─────────────┬───────────────────────────────────────────────┤
│             │                                               │
│             │  遅延設定パネル                                 │
│ サイドバー   │  ┌─────────────────────────────────────────┐  │
│             │  │ プリセット | スライダー | 経過時間表示    │  │
│  - EC       │  └─────────────────────────────────────────┘  │
│  - 管理画面  │                                               │
│  - SNS      │  メインコンテンツエリア                         │
│             │  （各画面のUI）                                 │
│  ─────────  │                                               │
│  [JP/EN]    │                                               │
│             │                                               │
└─────────────┴───────────────────────────────────────────────┘
```

### 5.2 サイドバー

| 要素 | 説明 |
|------|------|
| ロゴ/タイトル | 「API Delay Demo」など |
| ECサイト | ShoppingCart アイコン（Lucide） |
| 管理画面 | LayoutDashboard アイコン（Lucide） |
| SNS | MessageCircle アイコン（Lucide） |
| 区切り線 | セパレーター |
| 言語トグル | 「JP / EN」切り替えスイッチ |

- 選択中の画面はハイライト表示
- モバイルではハンバーガーメニューで開閉

### 5.3 遅延設定パネル（全画面共通・メインコンテンツ上部）

```
┌─────────────────────────────────────────────────────────────┐
│  [Instant] [Fast] [Normal] [Slow] [Very Slow]              │
│                                                             │
│  ─────────────────●──────────────────  1500ms              │
│  0ms                                              10000ms   │
│                                                             │
│  経過時間: 1,234ms  [👁]                                    │
└─────────────────────────────────────────────────────────────┘
```

#### プリセットボタン

| ラベル（日本語） | ラベル（英語） | 遅延時間 |
|-----------------|---------------|----------|
| 即時 | Instant | 0ms |
| 高速 | Fast | 100ms |
| 通常 | Normal | 500ms |
| 低速 | Slow | 2000ms |
| 超低速 | Very Slow | 5000ms |

- 選択中のプリセットはアクティブスタイル
- カスタム値の場合は全てのプリセットが非アクティブ

#### スライダー

| 項目 | 値 |
|------|-----|
| 最小値 | 0ms |
| 最大値 | 10000ms |
| 刻み | 100ms |
| 現在値表示 | スライダー横に数値表示 |

#### 経過時間表示

| 項目 | 仕様 |
|------|------|
| 表示形式 | 「経過時間: 1,234ms」/ 「Elapsed: 1,234ms」 |
| 更新頻度 | 10ms間隔でリアルタイム更新 |
| 表示/非表示 | 目のアイコン（Eye / EyeOff）でトグル |
| デフォルト | 非表示（EyeOffアイコン） |

---

## 6. 各画面の詳細仕様

### 6.1 ECサイト画面（`/ec`）

#### 6.1.1 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│ [🔍 検索バー________________________] [検索ボタン]           │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│ │ 商品1   │ │ 商品2   │ │ 商品3   │ │ 商品4   │            │
│ │ [画像]  │ │ [画像]  │ │ [画像]  │ │ [画像]  │            │
│ │ ¥1,000  │ │ ¥2,000  │ │ ¥3,000  │ │ ¥4,000  │            │
│ │[カート] │ │[カート] │ │[カート] │ │[カート] │            │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘            │
│                                                             │
│ ┌─────────┐ ┌─────────┐ ...                                │
│ │ 商品5   │ │ 商品6   │                                     │
│ └─────────┘ └─────────┘                                     │
├─────────────────────────────────────────────────────────────┤
│              < 1 2 3 4 5 ... 10 >                           │
└─────────────────────────────────────────────────────────────┘
```

#### 6.1.2 操作と遅延

| 操作 | トリガー | 遅延適用 | 表示 |
|------|---------|---------|------|
| 検索 | 検索ボタンクリック | あり | 商品グリッド全体がスケルトン → 結果表示 |
| データ取得 | ページ切り替え / 初期表示 | あり | 商品グリッド全体がスケルトン → 商品カード表示 |
| 保存 | 「カートに追加」ボタン | あり | ボタンがローディング状態 → 「追加済み」表示 |

#### 6.1.3 ダミーデータ

| 項目 | 仕様 |
|------|------|
| 商品総数 | 200件 |
| 1ページあたり | 20件 |
| ページ数 | 10ページ |
| 商品データ | 商品名、価格（¥1,000〜¥50,000ランダム）、画像（プレースホルダー） |

#### 6.1.4 検索機能
- 商品名での部分一致検索（フロントエンドフィルタリング）
- 検索実行時に遅延を適用
- 検索結果が0件の場合は「該当する商品がありません」表示

---

### 6.2 管理画面（`/admin`）

#### 6.2.1 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│ [🔍 検索_______________] [検索]    [+ 新規作成]             │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ID │ 名前      │ メール          │ 役割   │ 状態 │ 操作 │ │
│ ├────┼───────────┼─────────────────┼────────┼──────┼──────┤ │
│ │ 1  │ 田中太郎  │ tanaka@example  │ 管理者 │ 有効 │ [編集]│ │
│ │ 2  │ 鈴木花子  │ suzuki@example  │ 一般   │ 有効 │ [編集]│ │
│ │ 3  │ 佐藤次郎  │ sato@example    │ 一般   │ 無効 │ [編集]│ │
│ │ ...│ ...       │ ...             │ ...    │ ...  │ ...  │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ 1-20 of 200           < 1 2 3 4 5 ... 10 >                  │
└─────────────────────────────────────────────────────────────┘

[編集モーダル]
┌─────────────────────────────────────┐
│ ユーザー編集                    [×] │
├─────────────────────────────────────┤
│ 名前:  [___________________]        │
│ メール: [___________________]        │
│ 役割:  [管理者 ▼]                   │
│ 状態:  [● 有効 / ○ 無効]            │
├─────────────────────────────────────┤
│           [キャンセル] [保存]        │
└─────────────────────────────────────┘
```

#### 6.2.2 操作と遅延

| 操作 | トリガー | 遅延適用 | 表示 |
|------|---------|---------|------|
| 検索 | 検索ボタンクリック | あり | テーブル全体がスケルトン → 結果表示 |
| データ取得 | ページ切り替え / 初期表示 | あり | テーブル全体がスケルトン → データ表示 |
| 保存 | モーダルの「保存」ボタン | あり | 保存ボタンがローディング → モーダル閉じる＋トースト表示 |

#### 6.2.3 ダミーデータ

| 項目 | 仕様 |
|------|------|
| ユーザー総数 | 200件 |
| 1ページあたり | 20件 |
| ページ数 | 10ページ |
| カラム | ID, 名前, メール, 役割（管理者/一般）, 状態（有効/無効）, 操作 |

#### 6.2.4 編集機能
- 編集ボタンでモーダル表示（モーダル表示自体は遅延なし）
- 保存時に遅延を適用
- 保存完了後にトースト通知「保存しました」

---

### 6.3 SNS画面（`/sns`）

#### 6.3.1 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│ [🔍 ユーザー・ハッシュタグ検索_______________] [検索]       │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 投稿フォーム                                             │ │
│ │ [新しい投稿を入力...                                   ] │ │
│ │                                         [投稿する]       │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 👤 ユーザー名 @handle · 5分前                           │ │
│ │ 投稿内容がここに表示されます。#ハッシュタグ              │ │
│ │ [♡ 12] [💬 3] [🔄 5]                                    │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 👤 別のユーザー @another · 10分前                       │ │
│ │ これは別の投稿です。                                     │ │
│ │ [♡ 8] [💬 1] [🔄 2]                                     │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ...                                                         │
│                                                             │
│            [↓ さらに読み込む] または 無限スクロール          │
└─────────────────────────────────────────────────────────────┘
```

#### 6.3.2 操作と遅延

| 操作 | トリガー | 遅延適用 | 表示 |
|------|---------|---------|------|
| 検索 | 検索ボタンクリック | あり | タイムライン全体がスケルトン → 結果表示 |
| データ取得 | スクロール最下部到達 / 初期表示 | あり | 追加読み込み中はスケルトン（3件分）が下に表示 |
| 保存（投稿） | 「投稿する」ボタン | あり | ボタンがローディング → 投稿がタイムライン最上部に追加 |
| 保存（いいね） | ハートアイコンクリック | あり | アイコンがローディング → 数字増加＋塗りつぶし |

#### 6.3.3 ダミーデータ

| 項目 | 仕様 |
|------|------|
| 投稿総数 | 200件 |
| 初期表示 | 20件 |
| 追加読み込み | 20件ずつ |
| 投稿データ | ユーザー名、ハンドル、投稿日時、本文、いいね数、コメント数、リツイート数 |

#### 6.3.4 無限スクロール
- スクロール最下部に到達で自動読み込み
- 読み込み中は投稿カードのスケルトン（3件分）を表示
- 全200件読み込み完了後は「これ以上投稿はありません」表示

---

## 7. ローディング表示仕様

### 7.1 全画面スピナー
- 使用箇所: 初期ページ読み込み時
- shadcn/uiのSpinnerコンポーネント使用
- 画面中央に配置、背景は半透明オーバーレイ

### 7.2 スケルトンUI
- 使用箇所: 個々のコンテンツ読み込み時
- shadcn/uiのSkeletonコンポーネント使用
- 各コンテンツの形状に合わせたスケルトン

| 画面 | スケルトン形状 |
|------|---------------|
| EC | 商品カード型（画像部分は正方形、テキスト部分は横長バー） |
| 管理画面 | テーブル行（各カラムに横長バー） |
| SNS | 投稿カード型（アバター丸＋テキストバー複数行） |

### 7.3 ボタンローディング
- 使用箇所: 保存系操作のボタン
- ボタン内にスピナーを表示
- ボタンはdisabled状態

---

## 8. 多言語対応

### 8.1 対応言語

| 言語 | コード | URLパス |
|------|--------|---------|
| 日本語 | ja | `/jp/` |
| 英語 | en | `/en/` |

### 8.2 翻訳対象

- サイドバーメニュー
- 遅延設定パネル（プリセットラベル、経過時間ラベル）
- 各画面のUI要素（ボタン、ラベル、プレースホルダー）
- トースト通知メッセージ
- エラーメッセージ
- ダミーデータ（商品名、ユーザー名など）

### 8.3 実装方式
- i18n用のJSONファイルで翻訳管理
- React Contextで言語状態管理
- URLパスと言語トグルの双方向同期

---

## 9. レスポンシブデザイン

### 9.1 ブレークポイント

| 名称 | 幅 | レイアウト |
|------|-----|----------|
| Mobile | < 768px | サイドバー非表示（ハンバーガーメニュー）、1カラム |
| Tablet | 768px - 1024px | サイドバー縮小（アイコンのみ）、2カラムグリッド |
| Desktop | > 1024px | フルサイドバー、4カラムグリッド（EC）/ フルテーブル |

### 9.2 モバイル固有の対応

- 商品グリッド: 1列表示
- テーブル: 横スクロール or カード形式に変換
- モーダル: フルスクリーン表示
- 遅延設定パネル: スライダーはタッチ操作対応

---

## 10. 遅延シミュレーション実装

### 10.1 遅延関数

```typescript
const simulateDelay = (ms: number): Promise<void> => {
  return new Promise(resolve => setTimeout(resolve, ms));
};
```

### 10.2 経過時間計測

```typescript
const useElapsedTime = (isLoading: boolean) => {
  const [elapsed, setElapsed] = useState(0);
  const startTime = useRef<number | null>(null);

  useEffect(() => {
    if (isLoading) {
      startTime.current = Date.now();
      const interval = setInterval(() => {
        setElapsed(Date.now() - startTime.current!);
      }, 10);
      return () => clearInterval(interval);
    } else {
      setElapsed(0);
    }
  }, [isLoading]);

  return elapsed;
};
```

### 10.3 遅延適用タイミング
- 全ての「検索」「データ取得」「保存」操作の開始時に遅延を適用
- 遅延中はローディング状態をtrueに設定
- 遅延完了後にデータ更新・UI更新

---

## 11. 状態管理

### 11.1 グローバル状態（Context）

| 状態 | 型 | 説明 |
|------|-----|------|
| delay | number | 現在の遅延時間（ms） |
| language | 'jp' \| 'en' | 現在の言語 |
| theme | 'light' \| 'dark' | 現在のテーマ |
| isLoading | boolean | ローディング中フラグ |
| showElapsedTime | boolean | 経過時間表示フラグ |

### 11.2 各画面のローカル状態

#### ECサイト
- products: 商品リスト
- currentPage: 現在のページ番号
- searchQuery: 検索クエリ
- cart: カート内商品ID配列

#### 管理画面
- users: ユーザーリスト
- currentPage: 現在のページ番号
- searchQuery: 検索クエリ
- editingUser: 編集中ユーザー（モーダル用）

#### SNS
- posts: 投稿リスト
- loadedCount: 読み込み済み件数
- searchQuery: 検索クエリ
- newPostContent: 新規投稿内容

---

## 12. ディレクトリ構成

```
src/
├── components/
│   ├── layout/
│   │   ├── Sidebar.tsx
│   │   ├── Header.tsx
│   │   └── MainLayout.tsx
│   ├── common/
│   │   ├── DelaySettingsPanel.tsx
│   │   ├── LanguageToggle.tsx
│   │   ├── ThemeToggle.tsx
│   │   └── ElapsedTimeDisplay.tsx
│   ├── ec/
│   │   ├── ProductGrid.tsx
│   │   ├── ProductCard.tsx
│   │   ├── ProductSkeleton.tsx
│   │   └── SearchBar.tsx
│   ├── admin/
│   │   ├── UserTable.tsx
│   │   ├── UserTableSkeleton.tsx
│   │   ├── EditUserModal.tsx
│   │   └── SearchBar.tsx
│   └── sns/
│       ├── Timeline.tsx
│       ├── PostCard.tsx
│       ├── PostSkeleton.tsx
│       ├── PostForm.tsx
│       └── SearchBar.tsx
├── contexts/
│   ├── DelayContext.tsx
│   ├── LanguageContext.tsx
│   └── ThemeContext.tsx
├── hooks/
│   ├── useDelay.ts
│   ├── useElapsedTime.ts
│   └── usePagination.ts
├── i18n/
│   ├── ja.json
│   └── en.json
├── data/
│   ├── products.ts
│   ├── users.ts
│   └── posts.ts
├── pages/
│   ├── ECPage.tsx
│   ├── AdminPage.tsx
│   └── SNSPage.tsx
├── lib/
│   └── utils.ts
├── App.tsx
├── main.tsx
└── index.css
```

---

## 13. GitHub Pages デプロイ設定

### 13.1 vite.config.ts

```typescript
export default defineConfig({
  base: '/repository-name/', // GitHubリポジトリ名に変更
  plugins: [react()],
  build: {
    outDir: 'dist'
  }
});
```

### 13.2 ルーティング
- HashRouterを使用（GitHub Pagesは履歴APIをサポートしないため）
- 例: `https://username.github.io/repo-name/#/jp/ec?delay=1000`

### 13.3 GitHub Actions（.github/workflows/deploy.yml）

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npm run build
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

---

## 14. ダミーデータ生成仕様

### 14.1 商品データ（EC）

```typescript
interface Product {
  id: number;
  name: { ja: string; en: string };
  price: number;
  image: string; // プレースホルダーURL
}
```

- 200件生成
- 価格: 1,000円〜50,000円のランダム
- 画像: `https://via.placeholder.com/200x200?text=Product+{id}`

### 14.2 ユーザーデータ（管理画面）

```typescript
interface User {
  id: number;
  name: { ja: string; en: string };
  email: string;
  role: 'admin' | 'user';
  status: 'active' | 'inactive';
}
```

- 200件生成
- 日本語名: 姓名ランダム組み合わせ
- 英語名: First Last形式

### 14.3 投稿データ（SNS）

```typescript
interface Post {
  id: number;
  user: {
    name: { ja: string; en: string };
    handle: string;
    avatar: string;
  };
  content: { ja: string; en: string };
  createdAt: Date;
  likes: number;
  comments: number;
  retweets: number;
}
```

- 200件生成
- 投稿日時: 過去7日間のランダム
- いいね数: 0〜100のランダム

---

## 15. 実装上の注意点

### 15.1 パフォーマンス
- 商品グリッド・テーブルは仮想スクロール不要（20件/ページのため）
- SNSの無限スクロールは表示済み投稿を保持
- 画像は遅延読み込み（loading="lazy"）

### 15.2 アクセシビリティ
- ローディング中はaria-busy="true"
- スケルトンにはaria-label="読み込み中"
- キーボードナビゲーション対応
- カラーコントラスト比 WCAG AA準拠

### 15.3 エラーハンドリング
- 本プロジェクトでは実際のAPIコールがないためエラーは発生しない
- 不正なURLパラメータはデフォルト値にフォールバック

---

## 16. 開発・ビルド手順

```bash
# 依存関係インストール
npm install

# 開発サーバー起動
npm run dev

# ビルド
npm run build

# ビルド結果プレビュー
npm run preview
```

---

## 17. 必要なshadcn/uiコンポーネント

```bash
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add input
npx shadcn@latest add slider
npx shadcn@latest add skeleton
npx shadcn@latest add table
npx shadcn@latest add dialog
npx shadcn@latest add toast
npx shadcn@latest add toggle
npx shadcn@latest add sidebar
npx shadcn@latest add pagination
npx shadcn@latest add badge
npx shadcn@latest add avatar
npx shadcn@latest add select
npx shadcn@latest add label
npx shadcn@latest add textarea
npx shadcn@latest add separator
npx shadcn@latest add dropdown-menu
```

---

## 18. 完成イメージ

### お客様へのデモシナリオ例

1. **Fast (100ms)** で操作 → 「ほぼ即座に反応しますね」
2. **Normal (500ms)** で操作 → 「少し待ちますが許容範囲です」
3. **Slow (2000ms)** で操作 → 「明らかに遅いと感じます」
4. **Very Slow (5000ms)** で操作 → 「ユーザーは離脱するかもしれません」

→ 「御社のシステムではどのくらいの速度を目標にしますか？」と議論につなげる

---

以上